/**
 * Reserved for v2: typed clients for the real upstream APIs.
 * Kept as empty placeholder modules so the service layer can
 * reference them without import errors.
 *
 *  - NVD CVE 2.0        -> src/services/providers/nvd.ts
 *  - CISA KEV catalog   -> src/services/providers/cisaKev.ts
 *  - FIRST EPSS API     -> src/services/providers/epss.ts
 */
export {};
