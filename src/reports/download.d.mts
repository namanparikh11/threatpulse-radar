/**
 * V6.5 — Browser download helper (types).
 */
export function downloadFile(filename: string, body: string, mimeType: string): void;
export function openHtmlInNewTab(html: string, windowName?: string): Window | null;
export function buildReportFilename(report: any, ext: string): string;
