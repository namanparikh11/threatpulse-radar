/**
 * V6.5 — TypeScript declarations for the canonical
 * form. Mirrors the JS surface.
 */

export function canonicalizeReportBytes(report: any): string;
export function canonicalizeReportBytesWithIntegrity(report: any): string;
export function looksLikeIsoDate(s: string): boolean;
