/// <reference types="vite/client" />

// Vite-specific ambient declarations.
// The `vite/client` triple-slash reference adds the `import.meta.env`
// type and a few CSS-module / asset helpers. Used by the v5.0 service
// layer to read `VITE_DATASET_PROXY_URL` with a default.
